<%= form_for(@appeal) do |f| %>
  <% if @appeal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@appeal.errors.count, "error") %> prohibited this appeal from being saved:</h2>

      <ul>
      <% @appeal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :full_name %><br>
    <%= f.text_field :full_name %>
  </div>
  <div class="field">
    <%= f.label :dob %><br>
    <%= f.date_select :dob %>
  </div>
  <div class="field">
    <%= f.label :solicitor_name %><br>
    <%= f.text_field :solicitor_name %>
  </div>
  <div class="field">
    <%= f.label :provider_acct_num %><br>
    <%= f.text_field :provider_acct_num %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
